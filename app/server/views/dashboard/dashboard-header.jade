script
	var session = !{JSON.stringify(session)} ;
- var capitalize = function(s){return s.replace(/\w\S*/g, function(txt){return txt.charAt(0).toUpperCase() + txt.substr(1).toLowerCase();});}
div.navbar.navbar-fixed-top
	div.navbar-inner
		div.container
			ul.nav.pull-right
				li
					a#client-name
				li.divider-vertical
				li
					button#btn-login.btn.btn-primary
						i.icon-lock.icon-white
						| Log In
				li.divider-vertical
				li
					button#btn-about.btn.btn-primary
						i.icon-home.icon-white
						| About Bridge
				li.divider-vertical

include modals/about
include modals/welcome
include modals/goodbye
include modals/timeout