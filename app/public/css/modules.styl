/*

Modules are the reusable, modular parts of our design. They are the callouts, the sidebar sections, the product lists and so on. This is where the majority of your styles will go.

Examples of Module Styles:

    .folder {
        padding: 5px;
    }
    .folder-name {
        color: #FFF;
    }
    .folder-items {
        background: url(icon.png);
    }

Examples of Subclassing Modules:

    .pod { 
        width: 100%; 
    } 
    .pod input[type=text] { 
        width: 50%; 
    }

    .pod-constrained input[type=text] {
        width: 100%; 
    }

    .pod-callout { 
        width: 200px; 
    }
    .pod-callout input[type=text] { 
        width: 180px; 
    }

Usage of a sublcass in HTML would look like this:
    
    <div class="pod">...</div>
    <div class="pod pod-constrained">...</div>
    <div class="pod pod-callout">...</div> 

*/

@import 'mixins'

.menu-item
	position relative
	width 220px
	height 166px
	background-color #EBEBEB
	margin-bottom 30px
	round-corners 5px
.menu-chrome
	position absolute
	z-index 2
	top 0
	left 0
	width 100%
	height 100%
	border 1px solid #ACACAC
	box-shadow inset -1px -1px 1px rgba(255, 255, 255, 0.4), inset 1px 1px 1px rgba(255, 255, 255, 0.4)
	round-corners 5px
.menu-header
	height 40px
	background #DADADA
	border-bottom 1px solid #CBCBCB
	text-align center
.menu-thumbnail
	background-image url('../img/bkgd/menu-item-background.jpg')
	background-repeat repeat-x
	float left
	position relative
	width 160px
	height 130px
	border-right 1px solid #D9D9D9
.menu-thumbnail > img
	position absolute
	top 50%
	margin-top -55px
	left 50%
	margin-left -50px
.menu-progress
	background-image url('../img/bkgd/menu-item-background.jpg')
	background-repeat repeat-x
	float left
	position relative
	height 130px
	width 58px

.v-progress
	width 15px
	height 100px
	overflow hidden
	margin 13px auto
	round-corners 10px
	background #FFF
	box-shadow inset 0 1px 2px rgba(0, 0, 0, 0.1)

.v-progress-bar
	width 15px
	height 100%
	margin-top 50px
	background-color #0E90D2
	background-image -moz-linear-gradient(top,#149BDF,#0480BE)
	background-image -webkit-gradient(linear,0 0,0 100%,from(#149BDF),to(#0480BE))
	background-image -webkit-linear-gradient(top,#149BDF,#0480BE)
	background-image -o-linear-gradient(top,#149BDF,#0480BE)
	background-image linear-gradient(top,#149BDF,#0480BE)
	background-image -ms-linear-gradient(top,#149BDF,#0480BE)
	background-repeat repeat-x
	filter unquote('progid:dximagetransform.microsoft.gradient(startColorstr="#149bdf",endColorstr="#0480be",GradientType=0)')
	box-shadow inset 0 -1px 0 rgba(0, 0, 0, 0.15)
	transition height .6s ease




